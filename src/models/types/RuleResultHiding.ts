/**
 * Результат проверки правил типа HIDING
 */
type RuleResultHiding = {
    requisiteKey: number
    groupNumber: number
    hideState: boolean
    hintMessage: string
    parentsChain: RuleParentChain,
    ruleData: RuleData
}
