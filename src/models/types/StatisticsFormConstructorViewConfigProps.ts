import { FormInstance } from "antd"

/**
 * Тип для свойств компонента Настройки отображения элемента на форме сбора
 */
export type StatisticsFormConstructorViewConfigProps = {
  field?: string
  form: FormInstance
}
